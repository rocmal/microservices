{
  "name": "timescale-customer-sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",

    "connection.url": "jdbc:postgresql://timescaledb:5432/analytics",
    "connection.user": "admin",
    "connection.password": "admin123",

    "topics": "customers",
    "auto.create": "true",
    "auto.evolve": "true",

    "insert.mode": "upsert",
    "pk.mode": "record_key",
    "pk.fields": "customerId"
  }
}
